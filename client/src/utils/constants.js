const constants = Object.freeze({
  REQUEST_TIMEOUT: 20000,
  CLIENT_PORT: process.env.REACT_APP_CLIENT_SERVER_PORT,
  SERVER_PORT: process.env.REACT_APP_BACKEND_SERVER_PORT,
});

export default constants;
